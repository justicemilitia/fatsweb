import { TreeGridDragDirective } from './tree-grid-drag.directive';

describe('TreeGridDragDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeGridDragDirective();
    expect(directive).toBeTruthy();
  });
});
